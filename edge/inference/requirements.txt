# IntelliOptics 2.5 - Inference Service Dependencies
# YOLOE: Pure ONNX Runtime (no Ultralytics at runtime) — Apache 2.0
# VLM:   HuggingFace transformers + PyTorch (BSD/Apache 2.0)

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6  # Required for FastAPI file uploads

# ONNX Runtime for YOLOE detection
onnxruntime>=1.16.0,<1.20.0

# PyTorch + transformers for Moondream VLM
# Pin to CUDA 12.8 index — compatible with NVIDIA driver CUDA 12.x
--extra-index-url https://download.pytorch.org/whl/cu128
torch>=2.7.0,<3.0.0
torchvision>=0.22.0,<1.0.0
transformers>=4.51.1,<5.0.0
einops>=0.7.0  # Required by Moondream2 model
accelerate>=1.10.0  # Efficient model loading

# CV libraries
numpy>=1.24.0
opencv-python-headless>=4.10.0
pillow>=11.0.0

# HTTP client
httpx==0.25.0

# Configuration
pyyaml==6.0.1
python-dotenv==1.0.0

# Utilities
cachetools==5.3.2  # For model caching
