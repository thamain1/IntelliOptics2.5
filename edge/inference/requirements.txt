# IntelliOptics 2.5 - Inference Service Dependencies (Pure ONNX Runtime)
# No torch, no ultralytics, no transformers at runtime — 100% Apache 2.0

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6  # Required for FastAPI file uploads

# ONNX Runtime (pinned for moondream compatibility)
onnxruntime>=1.16.0,<1.20.0

# Moondream VLM (uses ONNX Runtime internally via OnnxVL)
moondream==0.0.5
tokenizers>=0.15.0

# CV libraries
numpy>=1.24.0
opencv-python-headless>=4.10.0
pillow>=10.4.0,<11.0.0

# HTTP client
httpx==0.25.0

# Configuration
pyyaml==6.0.1
python-dotenv==1.0.0

# Utilities
cachetools==5.3.2  # For model caching
