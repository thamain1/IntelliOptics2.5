# Azure (optional — kept for backward compat, not required)
# azure-servicebus>=7.13
# azure-identity==1.17.1
# azure-storage-blob==12.23.1
pydantic>=2.7
httpx>=0.27
python-dotenv>=1.0
pillow>=10.4
numpy>=2.0
SQLAlchemy>=2.0,<3
psycopg[binary]==3.1.19
requests
onnxruntime
opencv-python-headless

# The IntelliOptics SDK is installed separately during the image build
